import { HighlightBgDirective } from './highlight-bg.directive';

describe('HighlightBgDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBgDirective();
    expect(directive).toBeTruthy();
  });
});
